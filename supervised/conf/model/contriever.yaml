init:
  doc_model: 'facebook/contriever-msmarco'
  tokenizer: ${model.init.doc_model}
  device: 'cuda'
  pooling_mode: 'mean'
  embedding_size: 768
  save_model: 'contriever'
  specialized_mode: 'densec3_top1'
  max_tokenizer_length: 256
  normalize: False
  temperature: 0.05
adapters:
  num_experts_to_use: 6
  num_experts: 6
  residual: True
  latent_size: 192
  non_linearity: 'relu'
  use_adapters: True

continue_train: False
  